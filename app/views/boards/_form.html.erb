<%= form_with(model: board, local: true) do |form| %>
  <% if board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
      <% board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :component %>
    <%= form.number_field :component %>
  </div>

  <div class="field">
    <%= form.label :photo_table %>
    <%= form.file_field :photo_table %>
  </div>

  <div class="field">
    <%= form.label :doc_rule %>
    <%= form.file_field :doc_rule %>
  </div>

  <div class="field">
    <%= form.label :pieces_photos %>
    <%= form.file_field :pieces_photos, multiple: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
